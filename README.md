Just a sample ansible playbook with installing nginx, node and pm2 on a CentOS machine.

It uses vagrant for provisioning of virtual machines.

## Requirements

* Vagrant
* VirtualBox
* ansible
* python 2.x

